# ---- Core deps for Python >= 3.8 ----
requests>=2.31.0; python_version >= "3.8"
Pillow>=10.0.0; python_version >= "3.8"
tqdm>=4.66.0; python_version >= "3.8"

# ---- Legacy pins for Python 3.7 ----
# Pillow 10.x and requests 2.32+ no longer support Python 3.7, hence the upper limit constraint.
requests>=2.28.0,<2.32.0; python_version < "3.8"
Pillow>=9.1.0,<10.0.0; python_version < "3.8"
tqdm>=4.64.0,<4.67.0; python_version < "3.8"